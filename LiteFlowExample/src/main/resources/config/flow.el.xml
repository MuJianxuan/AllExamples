<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE flow PUBLIC  "liteflow" "liteflow.dtd">
<flow>

    <!--demo-->
    <chain name="chain1">
        THEN(
        a, b, WHEN(c,d)
        );
    </chain>

    <!-- 订单信息查询   -->
    <chain name="getOrderInfo">
        then(getOrderInfoCmp)
    </chain>

</flow>
